# 团队填写收缩到一个多维表格:方案(2026-09-16;店主已定:补货批次 `SKU-n｜名称·R2`、报销单/TikTok 两表/期初盘点团队部分都搬)

店主 9/16 两问:①系统里的 SKU/批次命名能不能沿用「上线前任务」表「项目」列的「SKU-n｜名称」;②能不能把团队要填、要确认的事都收到任务 base(`UubEbqdQBaI8Ybse3wrceBFOnhb`)这一个多维表格里,同一件事不多处填,尽量共享数据。

## 结论

- **命名可以统一,而且应该。** 以「项目」列的 `SKU-n｜名称` 作为**批次**的唯一键;内部代号 S-n 就是 SKU-n(核心 `stock.registry_tag` 已映射,任务名前缀 `S4·` 也一致)。要补一条规则:这张表里「一个项目 = 一个上市批次」,不是产品家族(莫奈 S1/S2 是一个产品两批,店主 9/12 定)。产品家族由「SKU主数据」维护(MONET 家族 = SKU-1 + SKU-2;MONET-4L = SKU-4)。**补货批次要定写法**,建议 `SKU-3｜查尔斯顿·R2`(同产品第二批),不要另起 SKU 号。
- **收缩到一个 base 可以做到:团队填的全进这一个;店主私有的钱不动。** 手段不是把所有列都塞进一张表,而是飞书的**关联字段 + 查找引用**:批次只在「项目」表填一次,其余表用关联指向它,数字互相引用不重复填。

## 现状:团队要填的入口散在哪

| 入口 | 在哪 | 谁填 | 问题 |
|---|---|---|---|
| 上线前任务(任务/花费/费用备注) | 任务 base | 许世然等 | 套数写在费用备注里靠解析(9/16「270 套」事故) |
| 头程与调拨登记 | 任务 base | 许世然 | 批次号自己选一遍 |
| 国内存货去向 | 任务 base | 团队 | 项目自己选一遍;达人寄样与 CRM/达人寄样表重复 |
| SKU其他费用 | 任务 base | 店主/团队 | 归属批次靠文字 |
| 报销单(表单) | 报销 base | 全员 | 独立 base,项目/批次没关联 |
| 批次表 人核列(套数/丢件/留国内/单位成本) | 店主私有 base | 店主代填 | 团队看不到,留国内现已改由登记表给 |
| TikTok单品月度(手工) | 店主私有 base | 李妍莹月导 | 团队进不了私有 base,要经店主 |
| TikTok 达人寄样表 | 妍莹表 | 李妍莹 | 与去向表「达人寄样」重复 |
| 期初盘点·2026-09-30 团队部分 | 店主私有 base | 许世然/李妍莹 | 同上 |
| 日报备注登记(履约豁免) | 日报侧 | 团队 | 单独一张 |
| 达人 CRM | 张勇的系统 | 张勇 | 不动 |

重复填写点:批次名在 5 张表各选一遍;套数在任务备注与批次表两处;留国内在批次表与登记表两处;寄样在去向表 / CRM / 达人寄样表三处;头程运费任务表与登记表都可能写。

## 目标结构:一个 base「产品与供应链」(现任务 base 改名)

- **「项目」表(新,枢纽)**:一行一个批次。人填:项目键(`SKU-n｜名称`)、产品家族(关联 SKU主数据)、工厂、下单日、计划开售日、**套数**(取代备注解析)、状态(在产/待发/在售/售罄)、单位落地成本·人核、头程丢件·人核、备注。🤖 列(分仓/成本/状态/回本)由 bot 写——等于把私有 base 的批次表搬过来。
- **其余表把「项目/批次号」单选改为关联→项目表**:上线前任务、头程与调拨登记、国内存货去向、SKU其他费用,以及搬进来的 报销单(保留表单)、TikTok结算月表、TikTok达人寄样(或并入去向表「达人寄样·TikTok」)、期初盘点·团队部分、备注登记。
- **共享靠查找引用/汇总**:项目表上直接显示 任务表投入合计、登记发出/在途、去向表出库、其他费用合计、报销归属合计;登记表行上引用项目的工厂/套数;去向表行上引用项目的采购每套(bot 派生列)。
- **写权限**:人填列团队编辑;🤖 列与 🤖 表用字段权限锁定只读;机器人只写 🤖。
- **店主私有 base 只留钱**:工行流水、垫付支出、工资/社保、假设与现金模型四表、回款/头寸/期间费用输出、科目表。SKU主数据(事实映射,不含钱)搬到团队 base。

## 迁移步骤(每步独立,不影响日报)

1. 店主定三件事:补货批次命名;报销单 / TikTok 两表 / 期初盘点团队部分 搬不搬;选一个业务空档做第 3 步。
2. 建「项目」表灌 6 行(人核值从批次表迁),bot 双写(批次表 + 项目表)7 天对比。
3. 四张现有表把单选改关联:飞书改字段类型会丢值 → 新建关联列、脚本按名称回填、旧列只读保留 7 天再删;改前存快照;子单明细🤖头程(张勇脚本写「批次号」)同步改。
4. 搬报销单(重建表单)、TikTok 结算月表、达人寄样、备注登记、期初盘点团队部分;脚本改读新表;旧表只读封存。
5. 私有 base 批次表退役(bot 停写);核心口径改:B3 套数取项目表(删备注解析)、D3/D4 不变;docs/finance-rules.md 与 rules.py 同步。
6. 变更播报覆盖新表(只进产品开发群);GUI 与报告显示名统一用项目键。

## 涉及的脚本

batch_master / batch_ledger / stock_balance / inv_snapshot / sku_report / rev_sku(TikTok 月表)/ tiktok_load / reimb_sync / biz_daily(备注登记)/ bitable_watch(播报)/ cash_model(不动,私有)。关联字段的 API 返回是 `[{record_ids, text}]`,读法与单选不同,统一走 `T()` 取 text。

## 风险与代价

- 改字段类型丢值:靠「新列 + 回填 + 旧列保留」规避,改前快照。
- 表单要重建一次,报销链接会变,要通知全员。
- 我这边约两天;团队没有额外填写,反而少填。

## 进度

- 2026-09-16 第 2 步完成:任务 base 建「项目」表 `tblr78ZlhNMl4nJa`(8 行:SKU-1~8;人填列 项目/内部代号/产品编码/名称/工厂/下单日/计划开售日/套数/状态/单位落地成本·人核/头程丢件·人核/分仓·国内办公室·人核/备注,🤖 列 14 个),人核值从私有 base 批次表迁入(S2 丢件 5;套数 300/500/600/500/0/300),备注标「9/16 由批次表迁入,请核」。batch_master 已双写 🤖 列到项目表(按内部代号匹配),与批次表并行 7 天对比后再切读。
- 2026-09-16 第 3 步(非破坏式)完成:四张表(上线前任务 / 头程与调拨登记 / 国内存货去向 / SKU其他费用)各加关联列「项目·关联」→ 项目表,按名称回填 127/17/23/20 行;`proj_link_sync.py` 每 2 小时双向同步(旧单选有值填关联;关联有值回填单选),用 DRB 身份写,变更播报按 operator 过滤不进群。新模块 `proj.py`:`tag_of(f)`(关联列 → 单选 → 批次号 → 旧批次,经核心 registry_tag)与 `sku_of(f)`(产品编码从项目表读);batch_master / batch_ledger / stock_balance / sku_report 的去向表与登记表读法全部切过去。**顺带修了第二处失效**:去向表的字段早已从「批次」改成「项目」(SKU-n｜名称),四个脚本还在读「批次」→ 去向表 23 行一直没被计入(S2 的 14 件达人寄样 + 6 件补寄、S3 的 2 行);切过去后 S2 国内现存 38→24、S3 36→34,寄样与补寄进批次账。
- 团队现在怎么填:四张表选「项目·关联」(从项目表挑),旧单选可不填,bot 会补齐;7 天后(9/23)删旧单选、把关联列改名「项目」。子单明细🤖头程 由张勇脚本写「批次号」,读法已兼容,不用改。
- 2026-09-16 第 4 步之一 报销搬完:方案里写的「报销 base 表单」其实 9/15 已换成 王艳婷的「发票统计」base 报销记录(无表单,直接填表);现在把 报销记录(56 行,104 个附件下载后重传,负责人/单选/日期原样,金额合计对平 13,462)与 历史报销待补🤖(19 行)搬进任务 base(`tblxBq9YOfj1vD6g` / `tblxNu9LEnvgvNbG`),报销记录加「项目·关联」并按 SKU成本归属 回填;reimb_base.json 指新表,bitable_watch 改盯新表(仍进报销登记群,用 operator 过滤 bot 写入),feishu-rerun.service 重启;老 base 两张表改名「弃用·」。reimb_sync 对新表跑通(56 笔、工行 89 笔配对、历史待补 19 笔已补 3)。`proj_link_sync.py` 改为按核心 registry_tag 映射(SKU-n｜名称 / S3-查尔斯顿 / S3 查尔斯顿 都认),新表自动纳入。
- 下一步(第 4 步余下):TikTok 结算月表、达人寄样、备注登记、期初盘点团队部分;脚本改读新表。

## 设计修正(2026-09-16 晚,店主反馈「多了好多列、bot 列没用、项目·关联 与 项目 重复」)

- **不用关联字段,「项目」单选就是外键。** 五张表的「项目·关联」已删,`proj_link_sync.py` 与 cron 撤掉;脚本经 `proj.py` 读单选「项目」/「批次号」,文本经核心 `registry_tag` 映射(`SKU-3｜查尔斯顿` / `S3-查尔斯顿` / `S3 查尔斯顿` 都认)。代价:项目表上做不了飞书原生的查找引用/汇总,合计数由 bot 写进 🤖 列代替。
- **项目表精简到 17 列**:人填 9(项目 / 产品编码 / 工厂 / 下单日 / 套数 / 状态 / 单位落地成本·人核 / 头程丢件·人核 / 备注)+ 🤖 8(分仓·FBA / 海外仓 / 在途 / 国内、未归位、单位落地成本、状态、派生更新)。删掉的:内部代号(由项目名得出)、名称(项目名里有)、计划开售日(任务表里程碑已有)、分仓·国内办公室·人核(登记表工厂→国内办公室行已给)、套数🤖 / 国内原留🤖 / 国内出库🤖 / 工厂待发🤖 / 剩余·实物🤖 / 派生说明🤖(GUI 点数字看来源已覆盖)。
- **以后的原则**:给团队表加列前先问店主;不做「过渡期并存列」;同一个键只有一个字段。

## 再修正(同晚,店主:「项目表里的机器人列为什么要出现?派生数据不应该落到这里」)

- 对。项目表是团队填事实的表,派生结果(分仓/成本/状态)有自己的家:首尔 derived 库(`批次主数据🤖`,按日可重放)+ 只读 GUI(点数字看来源)。项目表 8 个 🤖 列全删,batch_master 不再往项目表写;项目表现在 **9 列全是人填**:项目 / 产品编码 / 工厂 / 下单日 / 套数 / 状态 / 单位落地成本·人核 / 头程丢件·人核 / 备注。
- 团队表里保留 🤖 列的唯一理由是「团队在这张表里要据此行动」:上线前任务的 进度评级🤖/偏差天数🤖(店主 9/4 定,甘特用)、报销记录的 付款状态🤖(报销人看钱到没到)。分析性的派生数一律不进团队表。
- 第 5 步顺序因此改为:batch_master 改读项目表人填列(套数/状态/工厂/下单日/人核成本/丢件),派生只写 derived 库(私有 base 批次表的 🤖 列过渡期照写,下游 batch_ledger / stock_balance / sku_report 改读 derived 后退役);B3 删备注解析。
